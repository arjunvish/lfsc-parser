(check 
  (% v1 var
  (% v2 var
    (% p1 (holds (clc (pos v1) (clc (pos v2) cln)))
    (% p2 (holds (clc (pos v2) (clc (neg v1) cln)))
      (: ;(cnf_holds (cnfc (clc (pos v2) (clc (pos v2) cln)) cnfn))
      (cnf_holds (cnfc (clc (pos v2) cln) cnfn))
          (cnfc_proof _ _ _ (satlem_simplify _ _ _ (R _ _ p1 p2 v1) (\ p5 p5)) cnfn_proof)
          ))))))