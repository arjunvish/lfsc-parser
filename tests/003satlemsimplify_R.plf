(check 
  (% v1 var
  (% v2 var
    (% p1 (holds (clc (pos v1) (clc (pos v2) cln)))
    (% p2 (holds (clc (pos v2) (clc (neg v1) cln)))
      (: (holds (clc (pos v2) cln))
          (satlem_simplify _ _ _ (R _ _ p1 p2 v1)
            (\ p3 p3))))))))
